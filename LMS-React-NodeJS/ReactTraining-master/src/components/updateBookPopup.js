import React from 'react';
// import PropTypes from 'prop-types';
// import BookActions from '../actions/bookActions';

export class UpdateBookPopup extends React.Component{
   // constructor(props){
   //    super(props);
   //    this.state = {
   //       title: this.props.popup.book.title ? this.props.popup.book.title : "",
   //       author: this.props.popup.book.author ? this.props.popup.book.author: ""
   //    }
   //    this.handleChange = this.handleChange.bind(this);
   // }
   // handleSubmit(){
   //    var newBook = {
   //       book_id: this.props.popup.book.book_id,
   //       title: this.state.title,
   //       author: this.state.author
   //    }
   //    BookActions.updateBookPopup(newBook);
   // }
}
// UpdateBookPopup.protoType = {
//    popup: PropTypes.object.isRequired
// }